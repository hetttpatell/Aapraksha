<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aapraksha"
        tools:targetApi="31">

        <!-- Splash Screen Activity (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Aapraksha.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Activity -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <!-- Set PIN Activity -->
        <activity
            android:name=".SetPinActivity"
            android:exported="false" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Signup Activity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Dashboard Activity -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <!-- Emergency Contacts Activity -->
        <activity
            android:name=".EmergencyContactsActivity"
            android:exported="false" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- Permissions Activity -->
        <activity
            android:name=".PermissionsActivity"
            android:exported="false" />

        <!-- Edit Profile Activity -->
        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- SOS Triggered Activity -->
        <activity
            android:name=".SosTriggeredActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <!-- History Activity -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false" />


        <!-- Safe Screen Activity -->
        <activity
            android:name=".SafeScreenActivity"
            android:exported="false" />

        <!-- Network Alerts Activity -->
        <activity
            android:name=".NetworkAlertsActivity"
            android:exported="false" />

        <!-- Notification Settings Activity -->
        <activity
            android:name=".NotificationSettingsActivity"
            android:exported="false" />


        <!-- Location History Activity -->
        <activity
            android:name=".LocationHistoryActivity"
            android:exported="false" />

        <!-- Alerts Activity (Live SOS) -->
        <activity
            android:name=".AlertsActivity"
            android:exported="false" />

        <!-- Response Options Activity -->
        <activity
            android:name=".ResponseOptionsActivity"
            android:exported="false" />

        <!-- Alert Detail Page Activity -->
        <activity
            android:name=".AlertDetailPageActivity"
            android:exported="false" />

        <!-- Location Tracking Service -->
        <service
            android:name=".LocationTrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Volume Button SOS — Accessibility Service -->
        <service
            android:name=".VolumeButtonService"
            android:enabled="true"
            android:exported="true"
            android:stopWithTask="false"
            android:label="AapRaksha SOS (Volume Buttons)"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/volume_sos_service" />
        </service>

        <!-- SOS Lock Service — prevents leaving SOS screen -->
        <service
            android:name=".SosLockService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="shortService"
            android:stopWithTask="false" />

    </application>

</manifest>